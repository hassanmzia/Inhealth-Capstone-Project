from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams, PointStruct

client = QdrantClient(host="localhost", port=6333)

# Create Collections

# 1. Clinical Guidelines Collection
client.create_collection(
    collection_name="clinical_guidelines",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE),
    # Llama 3.2 embeddings are 1536-dimensional
)

# 2. Medical Literature Collection
client.create_collection(
    collection_name="medical_literature",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE),
)

# 3. Patient Clinical Notes Collection
client.create_collection(
    collection_name="patient_notes",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE),
)

# 4. Drug Information Collection
client.create_collection(
    collection_name="drug_information",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE),
)

# 5. Disease Knowledge Base Collection
client.create_collection(
    collection_name="disease_knowledge",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE),
)

# Example: Insert Clinical Guideline
points = [
    PointStruct(
        id=1,
        vector=guideline_embedding,  # Generated by Llama 3.2
        payload={
            "title": "ADA Diabetes Management Guidelines 2024",
            "organization": "American Diabetes Association",
            "section": "Medication Management",
            "content": "Metformin is recommended as first-line therapy for type 2 diabetes...",
            "recommendations": ["Start metformin at diagnosis", "Titrate to maximum tolerated dose"],
            "evidence_level": "A",
            "year": 2024,
            "url": "https://diabetesjournals.org/care/article/47/Supplement_1/S158/153954"
        }
    )
]
client.upsert(collection_name="clinical_guidelines", points=points)

# Example: Semantic Search for RAG
search_results = client.search(
    collection_name="clinical_guidelines",
    query_vector=query_embedding,  # User question embedded
    limit=5,
    with_payload=True
)